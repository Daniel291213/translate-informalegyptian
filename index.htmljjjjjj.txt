<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Egyptian Arabic Translator</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f4f4;
      padding: 20px;
      max-width: 600px;
      margin: auto;
    }
    textarea, select, button {
      width: 100%;
      margin-top: 10px;
      padding: 10px;
      font-size: 16px;
    }
    #output {
      background: #fff;
      padding: 10px;
      min-height: 80px;
      margin-top: 10px;
      white-space: pre-wrap;
    }
  </style>
</head>
<body>

<h2>🇪🇬 Egyptian Informal Arabic Translator</h2>

<select id="direction">
  <option value="ar-en">Egyptian Arabic → English</option>
  <option value="en-ar">English → Egyptian Arabic</option>
</select>

<textarea id="input" rows="4" placeholder="اكتب الكلام هنا... / Type here..."></textarea>

<button onclick="translateText()">Translate</button>

<div id="output"></div>

<script>
async function translateText() {
  const inputText = document.getElementById("input").value;
  const direction = document.getElementById("direction").value;
  const outputDiv = document.getElementById("output");

  outputDiv.innerText = "Translating...";

  const systemPrompt =
    direction === "ar-en"
      ? "You are a professional translator. Translate Egyptian informal Arabic (slang, daily speech) into natural English."
      : "You are a native Egyptian Arabic speaker. Translate English into informal Egyptian Arabic (عامية مصرية), not Modern Standard Arabic.";

  try {
    const response = await fetch("https://api.openai.com/v1/chat/completions", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": "Bearer YOUR_OPENAI_API_KEY"
      },
      body: JSON.stringify({
        model: "gpt-4o-mini",
        messages: [
          { role: "system", content: systemPrompt },
          { role: "user", content: inputText }
        ],
        temperature: 0.3
      })
    });

    const data = await response.json();
    outputDiv.innerText = data.choices[0].message.content;

  } catch (error) {
    outputDiv.innerText = "Error. Check API key or internet connection.";
  }
}
</script>

</body>
</html>